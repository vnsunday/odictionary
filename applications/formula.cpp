#include <stdio.h>
#include <stdlib.h>

void quality_form() 
{
    const char* azPosition1[][2] = {
        { "Before", "Noun" },
        { "After", "Verb" },
        { "After", "Noun" }
    };

    const char* szQualityAware[][2] = {
        { }
    };

    int n1 = sizeof(azPosition1)/sizeof(azPosition1[0]);
}

int main()
{
    return 0;
}